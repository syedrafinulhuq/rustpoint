# Rust 

## Course: Rust Crash Course

### Variables

1. Rust variables are by default immutable
2. If you don't define any datatype by default the varible type is ANY

````text

a. LET can use ANY data type, no data type is required.LET cannot be used outside of function as global variable.

example:

let getHello  = "hello world"
let mut getName = "rafi"


b. CONST needs data type and all uppercase variable name.And the data should be readable absolute number constant

example:

const WARP_DATA:f64 = 9.9 
